spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

app:
  jwt:
    secret: test-secret-key-for-jwt-minimum-256-bits-for-hmac-sha-testing
    expiration: 86400000
  cors:
    allowed-origins: http://localhost:5173
  pivot:
    max-result-rows: 10000
